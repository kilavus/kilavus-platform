= form_for @video do |f|
  - if @video.errors.any?
    #error_explanation
      %h2= "#{pluralize(@video.errors.count, "error")} prohibited this video from being saved:"
      %ul
        - @video.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :vimeoid
    = f.text_field :vimeoid
  .field
    = f.label :title
    = f.text_field :title
  .field
    = f.label :description
    = f.text_area :description
  .field
    = f.label :duration
    = f.text_field :duration
  .field
    = f.label :visualization
    = f.text_area :visualization
  .actions
    = f.submit 'Save'
